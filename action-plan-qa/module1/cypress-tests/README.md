# Cypress Tests for E‑Commerce App

## Prerequisites
- Node.js 18+
- The e‑commerce `index.html` (latest version with cart modal) must be in the **parent folder** of `cypress-tests` (or adjust the `start:app` script in `package.json`).

## Setup

```bash
npm install
# Playwright Tests for E‑Commerce App

## Prerequisites
- Node.js 18+
- The e‑commerce `index.html` (latest version with cart modal) must be in the **same folder** as this test project, OR adjust the `start:app` script in `package.json` to point to its location.

## Setup

```bash
npm install
npx playwright install

npx playwright test